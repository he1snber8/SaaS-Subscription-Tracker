// import { useState, useEffect } from 'react';\n// export function SubscriptionForm({ initial, onSubmit }) {\n//   const [model, setModel] = useState(initial);\n//   useEffect(() => setModel(initial), [initial]);\n//   const handleChange = e => setModel({ ...model, [e.target.name]: e.target.value });\n//   return (\n//     <form onSubmit={e => { e.preventDefault(); onSubmit(model); }}>\n//       {/* TODO: inputs for name, monthlyPrice */}\n//     </form>);\n// }